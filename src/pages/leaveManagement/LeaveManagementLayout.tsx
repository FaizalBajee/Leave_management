import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const LeaveManagementLayout = (props: Props) => {
    return (
        <><Outlet /></>
    );
};

export default LeaveManagementLayout;