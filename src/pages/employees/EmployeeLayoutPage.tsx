import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const EmployeeLayoutPage = (props: Props) => {
    return (
        <><Outlet /></>
    );
};

export default EmployeeLayoutPage;